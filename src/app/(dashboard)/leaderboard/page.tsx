import { PageHeader } from "@/components/crm/page-header";

export default function LeaderboardPage() {
  return <><PageHeader eyebrow="Leaderboard" title="Rep performance" description="Rep metrics will populate as calls and activities are logged." /><div className="card p-6 text-slate-300">Start logging calls in My Queue. This page is ready for activity-based rep stats once users are assigned.</div></>;
}
